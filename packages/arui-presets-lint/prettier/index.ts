import { type Config } from 'prettier';

export default {
    printWidth: 100,
    singleQuote: true,
    jsxSingleQuote: true,
    tabWidth: 4,
    trailingComma: 'all',
    endOfLine: 'auto',
} satisfies Config;
