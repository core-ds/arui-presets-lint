---
'@alfalab/stylelint-core-vars': major
---

Установлена минимальная версия Node.js у `@alfalab/stylelint-core-vars` до `>=22.12.0`. В `MIGRATION_GUIDE` для v11 добавлены предупреждения про drop Node 20, отключение `react/react-in-jsx-scope` / `react/jsx-uses-react` и новый плагин `react-you-might-not-need-an-effect`. Из рекомендуемого `lint:fix` убран `knip --fix`, добавлен отдельный скрипт `lint:unused:fix`
