---
'arui-presets-lint': minor
---

В пресет stylelint добавлен плагин `stylelint-order` и правила `order/properties-order` и `order/order`.
За счёт них выравнивается порядок css свойств и выстраивается единообразный порядок вложенности в блоке.
